BEGIN{
	totalsent=0;
	totalrecieved=0;
	totallost=0;
}
{
	packetType=$5
	event=$1
	if(packetType=="cbr")
	{
		if(event=="+")
			totalsent++;
		else if(event=="r")
			totalrecieved++;
		else if(event=="d")
			totallost++;
	}
}
END{
	printf("Total recieved: %d\n",totalRecieved);
	printf("Total Dropped: %d\n",totalLost);
	printf("Total sent: %d\n",totalsent);
}
